@model IEnumerable<OrdersModel.DomainClasses.Customer>

@{
    ViewBag.Title = "IndexKeepbup";
}

<h2>Index2</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<div ng-app="homeIndex">*@
    <div ng-controller="CustomerController as vm">
    <a class="navbar-brand" href="..\Home.html">{{Message}}</a>

    <table class="table">
        <thead>
            <tr>

                <th>1stName</th>
                <th>Last Name</th>
                <th class="pdsa-action-button-column">
                    Delete
                </th>
            </tr>
        </thead>

        <tr ng-repeat="customer in vm.customers">
            <td class="pdsa-action-button-column">
                <a data-ng-href="/Customer/Edit/{{customer.CustomerId}}"
                   ng-click="vm.editClick(customer.CustomerId)"
                   class="btn btn-default btn-sm">
                    <i class="glyphicon glyphicon-edit"></i>
                </a>
            </td>
            <td>{{customer.FirstName}}  </td>
            <td>{{customer.LastName}}  </td>
            <td class="pdsa-action-button-column">
                <a href="#"
                   ng-click="vm.deleteClick(product.ProductId)"
                   class="btn btn-default btn-sm">
                    <i class="glyphicon glyphicon-trash"></i>
                </a>
            </td>

        </tr>


    </table>
</div>
@section Scripts {

    <script src="~/Scripts/angular.min.js"></script>
<script src="~/Scripts/angular-route.js"></script>
    <script src="~/app/app.js"></script>
     
    <script src="~/app/customer2.controller.js"></script>

}
     
